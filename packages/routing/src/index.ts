export const PACKAGE_NAME = "@tidyr/routing" as const;

/**
 * Direct PancakeV2, Uniswap V3, and 0x quoting will be implemented in
 * Phase 11 against the verified addresses recorded in
 * docs/research/external-addresses.md. No quote logic ships here until
 * those integrations can be tested against real (or forked) endpoints —
 * this package must never fabricate a quote.
 */
export {};
