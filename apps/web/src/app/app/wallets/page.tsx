import { EmptyState } from "@/components/ui/empty-state";

export default function WalletsPage() {
  return (
    <EmptyState
      title="Wallet workspace"
      description="Multi-wallet connection, labeling, and live token scanning ship in the next build pass (F5–F9). This route is wired into the app shell now so navigation and layout are already correct."
    />
  );
}
