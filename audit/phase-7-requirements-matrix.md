# Phase 7 Adversarial Audit — Requirements Matrix

Independent verification of the 20 claims listed in the audit brief. Each row states
the claim, the independent verification method used (code inspection and/or command
re-execution, not trust in prior reports), and the verdict. "VERIFIED" means I
reproduced the evidence myself in this session; "VERIFIED (code)" means confirmed by
direct source reading without needing a command re-run.

| # | Claim | Method | Verdict |
|---|-------|--------|---------|
| 1 | No generic adapter registry exists | Read `SweepExecutor.sol`, `SweepPlanLib.sol` in full — no mapping, no `registerAdapter`/mutable-adapter storage. `AdapterKind` enum closed to `{PANCAKE_V2, UNISWAP_V3}`, resolved via `_adapterFor` to two `immutable` fields set only in the constructor. | VERIFIED |
| 2 | No `registerAdapter`/`removeAdapter` production selectors exist | Ran `forge test --match-test test_registerAdapterSelector_noLongerExists` and `test_removeAdapterSelector_noLongerExists` — both call the selectors via raw `.call()` and assert `ok == false`. Both PASS. Also `grep -rn "registerAdapter\|removeAdapter" src/` returns zero matches in production source. | VERIFIED |
| 3 | `SwapAction` uses closed `AdapterKind` values only | `SwapAction.adapterKind` is typed `SweepPlanLib.AdapterKind` (`enum { PANCAKE_V2, UNISWAP_V3 }`), ABI-encoded as `uint8`. No `address adapter` field anywhere in the struct. | VERIFIED (code) |
| 4 | Only immutable PancakeV2/UniswapV3 adapters are reachable | `PANCAKE_V2_ADAPTER`/`UNISWAP_V3_ADAPTER` are `public immutable IAdapter`, set once in the constructor, never reassigned anywhere in the contract (no setter function exists). `_adapterFor` is the only path to an adapter address and returns only these two. | VERIFIED (code) |
| 5 | Invalid adapter enum ordinals revert | Ran `forge test --match-test test_invalidAdapterKindOrdinal_revertsAtAbiDecode -vv` — constructs raw calldata with `adapterKind = 2` (out of `{0,1}` range) and confirms revert at ABI-decode time, before `executeSweep`'s body runs. PASS. | VERIFIED |
| 6 | Route data cannot select an arbitrary router or target | Read both adapters in full: `PancakeV2Adapter._pairFor` resolves pairs only via the immutable `FACTORY.getPair(...)`, never from `routeData` directly; `UniswapV3Adapter.swap` calls only the immutable `ROUTER.exactInput`. `routeData` supplies only a token path, validated against `allowedIntermediateAssets` and required to start/end at `tokenIn`/`tokenOut`. No `routeData` field ever becomes a call target or arbitrary address. | VERIFIED (code) |
| 7 | Adapter dependencies immutable, no proxy/upgrade path | `FACTORY`/`ROUTER`/`WMON` are all `immutable` in both adapters; `PERMIT2`/`WMON`/adapter addresses are `immutable` in `SweepExecutor`. `grep -rn delegatecall src/` → zero matches (only a doc-comment mention). No `Proxy`/`UUPSUpgradeable` base contract anywhere in `src/`. | VERIFIED (code + grep) |
| 8 | Permit2 uses `SignatureTransfer`/`permitWitnessTransferFrom` | `SweepExecutor._pullViaPermit2` calls `PERMIT2.permitWitnessTransferFrom(...)` with a `PermitBatchTransferFrom` — the `ISignatureTransfer` interface, not `IAllowanceTransfer`. No `approve`/`permit`-style standing-allowance call exists anywhere in `SweepExecutor.sol`. | VERIFIED (code) |
| 9 | Permit2 witness binds `executionPlanHash` | `TidyrWitness.hashWitness(executionPlanHash)` is passed as the `witness` argument to `permitWitnessTransferFrom`, with `WITNESS_TYPE_STRING` correctly completing Permit2's own typehash stub. `Permit2Witness.t.sol` (7/7 passing) exercises this against a real, unmodified Permit2 deployment via `deployCode`, including a tampered-witness failure case. | VERIFIED (code + test run) |
| 10 | `executionPlanHash` binds chainId, executor, owner, recipient, output token, nonce, deadline, action order, token, amount, AdapterKind, route, minimum output | Read `SweepPlanLib.hashPlan`/`_hashSwapActions` in full: binds `chainId`, `executor`, `owner`, `recipient`, `outputToken`, `deadline`, `nonce`, `displayManifestHash`, plus element-wise action-array hashes (order-sensitive since hashed by array index) covering `tokenIn`, `amountIn`, `adapterKind`, `minAmountOut`, `keccak256(routeData)`, `allowFailure` per swap. All 18 `SweepPlanLib.t.sol` sensitivity tests pass, each mutating exactly one field and asserting the hash changes. | VERIFIED (code + test run) |
| 11 | Solidity and TypeScript execution hashes match | Compared `SweepPlanLib.sol::hashPlan` field-for-field against `packages/transaction-review/src/executionPlanHash.ts::hashExecutionPlan` — identical field order and ABI types. Both sides assert the same literal constant `VECTOR_A_EXPECTED_HASH = 0xc277e8285240c296bf7df135856a7fad4a4e665f94c1f469e9cc6940f71e31d3`. Ran both suites independently: `SweepPlanLibTest` (18/18 pass, includes `test_vectorA_...`) and `executionPlanHash.test.ts` (20/20 pass, includes the same vector). | VERIFIED (code + test run, both languages) |
| 12 | Permit2 authorization for one plan/AdapterKind cannot execute another | Ran `test_signedAdapterKind_cannotBeSubstitutedAtExecution`, `test_planASignature_cannotAuthorizePlanB_differentRecipient`, `test_planASignature_cannotAuthorizePlanB_differentMinAmountOut`, `test_modifiedPlan_fails`, `test_signature_doesNotSurviveChainIdChange`, `test_signature_doesNotSurviveCrossExecutorReplay` — all PASS as part of the 152-test run. | VERIFIED |
| 13 | Pre-existing token/output-token/native-MON balances cannot leak to the active caller | Read `executeSweep`'s baseline/delta accounting in full (per-token `baselines[]`, `settlementBaseline`, `nativeBaseline`, all snapshotted before the Permit2 pull). Ran `test_preExistingExecutorBalance_isNotSweptIntoPlan`, `testFuzz_preExistingBalance_neverAttributedToPlan` (10,000/10,000 runs, PASS), and `invariant_executorRetainsNoTouchedTokenBalance` (8,192 calls, 0 reverts, PASS). | VERIFIED |
| 14 | Optional action failure fully reverts that subcall | `_executeSwap`'s `catch` branch reverts only via `forceApprove(adapterAddress, 0)` cleanup and returns `false` when `action.allowFailure == true`; the adapter's own `swap()` call already reverted atomically, so nothing from that subcall persists. Ran `test_allowFailure_adapterReverts_planContinuesAndReturnsInput` — PASS. | VERIFIED |
| 15 | Required action failure reverts the whole plan | `_executeSwap`'s `catch` branch re-reverts with the bubbled reason via inline assembly when `allowFailure == false`. Ran `test_requiredSwapFails_revertsEntirePlan` — PASS. | VERIFIED |
| 16 | Successful execution retains no active-plan funds | `_returnRemainders` sweeps every touched token's balance above its pre-pull baseline back to `plan.owner` at the end of every successful call. Ran `invariant_executorRetainsNoTouchedTokenBalance` (8,192 calls, 0 reverts) and `testFuzz_transferAndDiscard_neverRetainsFunds` (10,000/10,000 runs) — both PASS. | VERIFIED |
| 17 | Configuration freeze is irreversible and complete | `configurationFrozen` (executor) and each adapter's own `configurationFrozen` are `bool`s set only by `freezeConfiguration()`, which never resets to `false` — no `unfreeze` function exists anywhere in `src/`. Executor's `freezeConfiguration()` additionally requires both adapters to have already frozen themselves (`AdapterNotYetFrozen` guard). Ran `test_freezeConfiguration_revertsUnlessBothAdaptersFrozen`, `test_swapsUnaffectedByFreezeState` — PASS. | VERIFIED |
| 18 | Multicall3 remains read-only, cannot be selected as adapter or spender | Constructor rejects Multicall3's real verified address (`0xcA11bde05977b3631167028862bE2a173976CA11`) in either adapter slot (`AdapterIsMulticall3`). No approval/spender path exists outside the two fixed adapters (only `forceApprove(adapterAddress, ...)` inside `_executeSwap`, scoped to `PANCAKE_V2_ADAPTER`/`UNISWAP_V3_ADAPTER`). Fork test `test_multicall3Address_matchesSweepExecutorConstant` confirms the constant matches the live, verified Multicall3 deployment on Monad mainnet — PASS (live RPC). | VERIFIED (code + live fork test) |
| 19 | Phase 9 requires deployed adapter bytecode, constructor args, immutable dependencies, and proxy checks — not only address equality | Read `docs/requirements-traceability.md` Section 2's "immutable adapter slots verified" row in full: mandates (1) read-back address equality, (2) independent `extcodesize` check, (3) runtime-bytecode comparison against the compiled artifact (masking immutable-argument byte ranges), (4) constructor-argument reconstruction, (5) each adapter's own immutable dependencies checked against `docs/research/external-addresses.md`, (6) EIP-1167/EIP-1967 proxy-pattern rejection, (7) full recording in `deployments/mainnet.json`, (8) hard abort (not a warning) on any mismatch. This is substantively more than address equality. | VERIFIED (doc read) |
| 20 | No Phase 8, deployment, or frontend work was introduced | `git diff 1a5f70c...3290568 --stat` shows only `packages/contracts/{src,test}`, `docs/`, `artifacts/security/`, root-level report/status docs, and `.gitignore` changed — zero touches to `apps/`, `deployments/`, `packages/routing`, `packages/execution`, or any frontend path. `apps/api`/`apps/indexer` predate this diff range (introduced in `bd53ed1`, before the Phase 7 window) and were untouched by it. `PHASE_7_SECURITY_COMPLETION_REPORT.md` §17 explicitly states "Phase 8 (mainnet deployment tooling) may not begin." No deployment transaction, `deployments/mainnet.json`, or `apps/web` product code exists. | VERIFIED (git diff + doc read) |

**Result: 20/20 claims independently verified as accurate.** No claim in the audit
brief was found to be false, overstated in a security-relevant way, or unreproducible.
